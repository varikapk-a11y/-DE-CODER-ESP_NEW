# -DE-CODER-ESP_NEW
Эти блоки предназначены для работы поверх стандартного протокола ESP-NOW, чтобы преодолеть его ограничение на количество каналов и организовать адресную доставку данных в сети из нескольких устройств.
CODER>ESP_NEW — для упаковки данных с нескольких датчиков в один бинарный пакет с уникальными ключами.
DECODER_ESP_NEW — для приёма этого пакета, его разбора и распределения данных по отдельным выходам.
Архитектура решения:
На передающей стороне:
[Датчик 1] → [Вход 1 кодера] \
[Датчик 2] → [Вход 2 кодера] → [CODER>ESP_NEW] → [Стандартный блок ESP-NOW] → (радиоканал)
[Датчик N] → [Вход N кодера] /
На приёмной стороне:                                           → [Выход 1] → [Nextion/Дисплей]
(Радиоканал) → [Стандартный блок ESP-NOW] → [DECODER_ESP_NEW] → [Выход 2] → [Nextion/Дисплей]
                                                               → [Выход N] → [Другие устройства]
Ключевые достижения
1. Создан блок CODER>ESP_NEW
Входы (7): SendTrigger (Boolean), Input1_float, Input2_float, Input3_Float, Input1_int, Input2_int, Input3_int

Выходы (2):

Out_string (String) — для отладки в монитор порта

Out_Byte (Byte Array) — бинарные данные для ESP-NOW

# Структура данных: Определена структура SensorPacket размером 7 байт:
struct SensorPacket {
    uint8_t key;      // Ключ датчика (1 байт)
    uint16_t sensorID;// ID датчика (2 байта)
    float value;      // Значение (4 байта)
};
# Логика работы: 
По сигналу SendTrigger упаковывает данные 6 датчиков в буфер из 6 структур
(42 байта) с уникальными ключами:

Float датчики: ключи 0xF1, 0xF2, 0xF3

Int датчики: ключи 0x11, 0x12, 0x13
# Создан блок DECODER_ESP_NEW
Входы: IN_ByteArray (Byte Array), IN_Length (Integer), IN_Trigger (Boolean)

Выходы: Output1_float, Output2_float, Output3_float, Output1_int, Output2_int, Output3_int + отладочный String

Структура данных: Полностью идентична структуре в кодере (критически важно!)

Логика работы: При получении буфера разбирает его по ключам и направляет значения на соответствующие выходы
# Решённые технические проблемы
Несовместимость типов данных в FLProg:

Зелёные линии = String данные

Чёрные линии = Integer/Float/Boolean данные

Синие линии = Byte Array данные

Для соединения блоков типы данных должны совпадать

Организация тестирования:

Создан тестовый проект для проверки кодера и декодера на одной плате

Использованы виртуальные UART-порты для передачи данных внутри платы

Настроен вывод отладочной информации в монитор порта
# Текущий статус
Блок CODER>ESP_NEW полностью готов и протестирован (формирует корректные пакеты)

Блок DECODER_ESP_NEW создан, но требует финальной настройки и тестирования

Определён чёткий формат пакета (42 байта) и система ключей

Решены вопросы совместимости типов данных между блоками

📋 Что предстоит сделать (Часть 2)
Настроить вывод отладочной информации из декодера в монитор порта

Протестировать полный цикл: кодер → ESP-NOW → декодер

Интегрировать блоки в реальный проект "Умного дома"

Оптимизировать структуру пакета при необходимости

💡 Ключевой принцип
Идентичность структур данных в кодере и декодере — основа работоспособности системы. Любое расхождение в порядке полей, их типах или размерах приведёт к невозможности разбора пакета.

Этот подход позволяет создавать масштабируемые сети устройств на ESP32 с адресной доставкой данных, что особенно актуально для проектов умного дома с множеством датчиков и исполнительных устройств.

## Новая логика работы блока
Блок должен:

Постоянно отслеживать 6 входов (3 float, 3 int).

Сравнивать их текущие значения с предыдущими, сохранёнными в памяти.

При обнаружении изменения на любом входе:

Формировать короткое сообщение (не весь пакет из 6 датчиков).

Передавать это сообщение в ESP-NOW для отправки.

Запоминать новое значение, чтобы не отправлять его повторно.
#  Кратко, как я теперь понимаю полную систему:
На стороне ПЕРЕДАТЧИКА (Transmitter):

У вас есть несколько датчиков (3 float, 3 int), подключенных к одной плате ESP32-C3.

Новый блок CODER>ESP_NEW должен сканировать их, и при изменении любого значения формировать небольшой пакет.

Пакет содержит: Ключ датчика (например, 0xF1) + Новое значение.

Этот пакет передаётся в блок ESP-NOW Sender, который отправляет его по одному радиоканалу на приёмник.

На стороне ПРИЁМНИКА (Receiver):

Другая плата ESP32-C3 постоянно слушает тот же канал ESP-NOW через блок ESP-NOW Receiver.

Второй новый блок DECODER_ESP_NEW принимает сырые данные от ESP-NOW Receiver.

Он смотрит на ключ в пакете (0xF1, 0x11 и т.д.) и направляет значение (value) на соответствующий выход блока (out_sensor1_float, out_sensor3_int и т.д.).

Эти выходы уже подключены к разным элементам (ID) экрана Nextion, и каждое значение попадает точно в свой текстовый бокс, индикатор или график.

Итог: Вы создаёте пару умных блоков-адаптеров (кодер/декодер), которые превращают разношёрстные данные с датчиков в аккуратный поток по ESP-NOW и обратно раздают их по нужным адресам на дисплее. Один канал ESP-NOW — много данных, точная доставка.

Теперь, с этой полной картиной, я готов помочь доделать оба блока корректно и без лишних сложностей. Согласны продолжить?
#  ФИНАЛЬНЫЙ АРХИВ ПРОЕКТА "ESP-NOW КОДЕР/ДЕКОДЕР ДЛЯ NEXTION"
🎯 1. ЦЕЛЬ ПРОЕКТА
Создать в FLProg два пользовательских блока (CODER>ESP_NEW и DECODER_ESP_NEW) для передачи данных от группы датчиков по одному каналу ESP-NOW с точной адресной доставкой значений на экран Nextion.

Архитектура:

Передатчик (Transmitter): Датчики → CODER>ESP_NEW (сканирует изменения, формирует пакет с ключом) → ESP-NOW Sender.

Приёмник (Receiver): ESP-NOW Receiver → DECODER_ESP_NEW (читает ключ, направляет значение на нужный выход) → Экран Nextion (разные ID).

📝 2. КЛЮЧЕВОЙ КОД ДЛЯ СТРУКТУРЫ (IDENTICAL ДЛЯ ОБОИХ БЛОКОВ)
Размещается в DeclareSection (начало):

cpp
#pragma pack(push, 1)
struct SensorPacket {
    uint8_t key;       // Ключ датчика: 0xF1, 0xF2, 0xF3, 0x11, 0x12, 0x13
    uint16_t sensorID; // ID датчика: 101, 102, 103, 201, 202, 203
    float value;       // Значение датчика (float для всех типов)
};
#pragma pack(pop)
const int SENSOR_COUNT = 6;
const int PACKET_BUFFER_SIZE = SENSOR_COUNT * sizeof(SensorPacket); // 42 байта
🔧 3. НАСТРОЙКА БЛОКА CODER>ESP_NEW (ПЕРЕДАТЧИК)
Назначение: Сканирует 6 входов, при изменении формирует пакет для отправки.

Вкладка редактора	Что настроить
Основные настройки	Имя блока: CODER>ESP_NEW
Общие условия	Условие onExecute: ЦПУ проекта == ESP32
Входы (Inputs)	in_sensor1_float, in_sensor2_float, in_sensor3_float (тип Float)
in_sensor1_int, in_sensor2_int, in_sensor3_int (тип Integer)
Выходы (Outputs)	out_debug_str (тип String)
out_packet (тип Integer – для передачи указателя)
DeclareSection	Код из п.2 + объявления для хранения старых значений: float old_f1, old_f2, old_f3; int old_i1, old_i2, old_i3;
LoopSection	Основная логика (код ниже).

# Код для LoopSection (начало) блока CODER>ESP_NEW:

// === ВСПОМОГАТЕЛЬНАЯ ФУНКЦИЯ ДЛЯ ФОРМИРОВАНИЯ СТРОКИ ===
}
void sendSensorUpdate(String key, float value) {
    out_espnow_string = key + ":" + String(value, 2);
    out_debug_str = "Sent -> " + out_espnow_string;
}
// ИНИЦИАЛИЗАЦИЯ ПРИ ПЕРВОМ ЗАПУСКЕ
if (firstRun) {
    old_f1 = in_sensor1_float;
    old_f2 = in_sensor2_float;
    old_f3 = in_sensor3_float;
    old_i1 = in_sensor1_int;
    old_i2 = in_sensor2_int;
    old_i3 = in_sensor3_int;
    firstRun = false;
    out_debug_str = "Coder INIT";
    return;
}

// ОСНОВНАЯ ЛОГИКА: ПРОВЕРЯЕМ ИЗМЕНЕНИЯ И ФОРМИРУЕМ СТРОКУ
if (in_trigger) {
    // --- Проверка FLOAT датчиков ---
    if (in_sensor1_float != old_f1) {
        sendSensorUpdate("F1", in_sensor1_float);
        old_f1 = in_sensor1_float;
        return;
    }
    if (in_sensor2_float != old_f2) {
        sendSensorUpdate("F2", in_sensor2_float);
        old_f2 = in_sensor2_float;
        return;
    }
    if (in_sensor3_float != old_f3) {
        sendSensorUpdate("F3", in_sensor3_float);
        old_f3 = in_sensor3_float;
        return;
    }

    // --- Проверка INT датчиков (преобразуем int во float) ---
    if (in_sensor1_int != old_i1) {
        sendSensorUpdate("I1", (float)in_sensor1_int);
        old_i1 = in_sensor1_int;
        return;
    }
    if (in_sensor2_int != old_i2) {
        sendSensorUpdate("I2", (float)in_sensor2_int);
        old_i2 = in_sensor2_int;
        return;
    }
    if (in_sensor3_int != old_i3) {
        sendSensorUpdate("I3", (float)in_sensor3_int);
        old_i3 = in_sensor3_int;
        return;
    }
    out_debug_str = "No changes"; // Сообщение, если триггер был, но изменений нет
}

// === ОБЪЯВЛЕНИЕ БУФЕРА (Добавить в DeclareSection) ===
SensorPacket packetBuffer; // Буфер для одного пакета, а не для массива
🛠️ 4. НАСТРОЙКА БЛОКА DECODER_ESP_NEW (ПРИЁМНИК)
Назначение: Принимает пакет по указателю, читает ключ, направляет значение на нужный выход.

Вкладка редактора	Что настроить
Основные настройки	Имя блока: DECODER_ESP_NEW
Общие условия	Условие onExecute: ЦПУ проекта == ESP32
Входы (Inputs)	in_packet (тип Integer – принимает указатель)
in_packet_len (тип Integer, можно не использовать)
Выходы (Outputs)	out_sensor1_float, out_sensor2_float, out_sensor3_float (тип Float)
out_sensor1_int, out_sensor2_int, out_sensor3_int (тип Integer)
out_debug_str (тип String)
DeclareSection	Тот же самый код структуры SensorPacket (как в п.2)
LoopSection	Основная логика разбора (код ниже).
Код для LoopSection (начало) блока DECODER_ESP_NEW:

cpp
// === ПРЕОБРАЗОВАНИЕ УКАЗАТЕЛЯ И РАЗБОР ПАКЕТА ===
SensorPacket* receivedPacket = (SensorPacket*)in_packet; // Критически важная строка

// Распределяем значение по ключу
switch (receivedPacket->key) {
    case 0xF1:
        out_sensor1_float = receivedPacket->value;
        out_debug_str = "Rcvd F1: " + String(receivedPacket->value, 2);
        break;
    case 0xF2:
        out_sensor2_float = receivedPacket->value;
        out_debug_str = "Rcvd F2: " + String(receivedPacket->value, 2);
        break;
    case 0xF3:
        out_sensor3_float = receivedPacket->value;
        out_debug_str = "Rcvd F3: " + String(receivedPacket->value, 2);
        break;
    case 0x11:
        out_sensor1_int = (int)receivedPacket->value;
        out_debug_str = "Rcvd I1: " + String(out_sensor1_int);
        break;
    case 0x12:
        out_sensor2_int = (int)receivedPacket->value;
        out_debug_str = "Rcvd I2: " + String(out_sensor2_int);
        break;
    case 0x13:
        out_sensor3_int = (int)receivedPacket->value;
        out_debug_str = "Rcvd I3: " + String(out_sensor3_int);
        break;
    default:
        out_debug_str = "Rcvd Unknown Key: " + String(receivedPacket->key, HEX);
}
✅ 5. КРИТИЧЕСКИ ВАЖНЫЕ МОМЕНТЫ (ЧТО ПРОВЕРИТЬ)
Тип выхода/входа packet: Должен быть Integer, а не Byte, Char или Data Buffer. Это обходной путь для передачи указателя на память.

Идентичность структур: Код в DeclareSection (с #pragma pack) должен быть одинаковым в обоих блоках.

Ключи и ID: Должны чётко соответствовать между кодером и декодером (0xF1 → F1, 0x11 → I1).

Подключение в схеме:

CODER>ESP_NEW.out_packet → ESP-NOW Sender.Data (возможно, потребуется конвертер типа).

ESP-NOW Receiver.Data → DECODER_ESP_NEW.in_packet.

🚀 6. ЧТО ДЕЛАТЬ ДАЛЕЕ В НОВОМ ЧАТЕ
Завершите настройку входов/выходов для CODER>ESP_NEW по таблице выше.

Скопируйте и вставьте обновлённый код LoopSection для кодера (с логикой отслеживания изменений).

Создайте блок DECODER_ESP_NEW по аналогии.

Соберите тестовую схему с Serial Print для отладки строк out_debug_str.

Протестируйте передачу одного изменяющегося значения от датчика → Nextion.

# новое в проекте
от исправленная правильная схема подключения для ESP8266:
# Сторона передатчика (Transmitter):
[Датчик 1] → [Вход 1 кодера]
[Датчик 2] → [Вход 2 кодера] → [CODER>ESP_NEW] → [Пакет (Byte Array)] → [ESP_NOW ESP8266 (Режим "Передача")]
[Датчик 3] → [Вход 3 кодера] ↓
[Отладка (String)] → [Монитор порта]
# Сторона приемника (Receiver):
[ESP_NOW ESP8266 (Режим "Прием")] → [Пакет (Byte Array)] → [DECODER_ESP_NEW] → [Выход 1] → [Nextion/Дисплей]
                                                                                [Выход 2] → [Nextion/Дисплей]
                                                                                [Выход 3] → [Nextion/Дисплей]
# Правильная и простая архитектура
Из пункта 2.2. описания ясно:

Входы (выходы) - в блоке предусмотрены 8 входов (выходов)... 4 входа (выхода) int, 2 входа (выхода) string, 2 входа (выхода) float, 2 входа (выхода) bool.

Это значит, что стандартный блок ESP_NOW ESP8266 использует простые типы данных (int, float, string, bool), а не сложные структуры или массивы байт.
#  Как правильно передать данные от CODER> к ESP_NOW
Судя по всему, этот блок ESP_NOW ESP8266 — не низкоуровневый драйвер, а высокоуровневая обертка, которая сама управляет упаковкой данных в пакеты ESP-NOW. Поэтому наша задача упрощается: CODER> должен передавать данные не в бинарном формате, а в том виде, в котором их ждет ESP-NOW блок.

Проще всего передавать данные через строку (String), как в моем первом предложении (формат "F1:25.5").

Или, если в проекте требуется передавать все 6 значений сразу, можно вывести их на 6 отдельных выходов блока CODER с типами int и float и подключить их к соответствующим входам блока ESP_NOW ESP8266.
# NOW
Теперь я полностью понял архитектуру. Цель проекта — расширить количество входов для блока ESP_NOW ESP8266, объединив данные от многих датчиков в один пакет, который можно передать через один вход ESP-NOW блока.

Правильная задача:
CODER>ESP_NEW должен упаковывать данные от множества датчиков в единый поток, который можно подать на один из доступных типизированных входов блока ESP_NOW ESP8266.
#  Решение: Использование входа String
У блока ESP_NOW ESP8266 есть входы типа String.

Строка может содержать структурированные данные.

Это полностью соответствует описанию проекта.
Итоговая архитектура:
[Датчик 1 Float] ───┐
[Датчик 2 Float] ───┤
[Датчик 3 Float] ───┼──> [CODER>ESP_NEW] ──> (Выход String) ──> [Вход String ESP_NOW ESP8266]
[Датчик 1 Int]   ───┤
[Датчик 2 Int]   ───┤
[Датчик 3 Int]   ───┘
# Формат строки:
CODER будет формировать строку вида F1:25.50;F2:30.10;I1:100;I2:200;..., где:

F1, F2 — ключи для float-датчиков

I1, I2 — ключи для int-датчиков

Декодер на стороне приемника будет разбирать эту строку по разделителю ; и ключам, направляя значения на нужные выходы.

Нужно ли мне сразу переписать код CODER>ESP_NEW для формирования такой строки? После этого мы создадим DECODER_ESP_NEW.
# DeclareSection
// Переменные для хранения предыдущих значений датчиков
float old_f1 = 0, old_f2 = 0, old_f3 = 0;
int old_i1 = 0, old_i2 = 0, old_i3 = 0;
bool firstRun = true;
# LoopSection
// ИНИЦИАЛИЗАЦИЯ ПРИ ПЕРВОМ ЗАПУСКЕ
if (firstRun) {
    old_f1 = in_sensor1_float;
    old_f2 = in_sensor2_float;
    old_f3 = in_sensor3_float;
    old_i1 = in_sensor1_int;
    old_i2 = in_sensor2_int;
    old_i3 = in_sensor3_int;
    firstRun = false;
    out_debug_str = "Coder INIT";
    return;
}

// ОСНОВНАЯ ЛОГИКА: ПРОВЕРЯЕМ ИЗМЕНЕНИЯ И ФОРМИРУЕМ СТРОКУ
if (in_trigger) {
    // --- Проверка FLOAT датчиков ---
    if (in_sensor1_float != old_f1) {
        sendSensorUpdate("F1", in_sensor1_float);
        old_f1 = in_sensor1_float;
        return;
    }
    if (in_sensor2_float != old_f2) {
        sendSensorUpdate("F2", in_sensor2_float);
        old_f2 = in_sensor2_float;
        return;
    }
    if (in_sensor3_float != old_f3) {
        sendSensorUpdate("F3", in_sensor3_float);
        old_f3 = in_sensor3_float;
        return;
    }

    // --- Проверка INT датчиков (преобразуем int во float) ---
    if (in_sensor1_int != old_i1) {
        sendSensorUpdate("I1", (float)in_sensor1_int);
        old_i1 = in_sensor1_int;
        return;
    }
    if (in_sensor2_int != old_i2) {
        sendSensorUpdate("I2", (float)in_sensor2_int);
        old_i2 = in_sensor2_int;
        return;
    }
    if (in_sensor3_int != old_i3) {
        sendSensorUpdate("I3", (float)in_sensor3_int);
        old_i3 = in_sensor3_int;
        return;
    }
    out_debug_str = "No changes"; // Сообщение, если триггер был, но изменений нет
}

// === ВСПОМОГАТЕЛЬНАЯ ФУНКЦИЯ ДЛЯ ФОРМИРОВАНИЯ СТРОКИ ===
void sendSensorUpdate(String key, float value) {
    // Формируем строку в формате "КЛЮЧ:ЗНАЧЕНИЕ" (например, "F1:25.50")
    out_espnow_string = key + ":" + String(value, 2);
    // Для отладки выводим более подробную информацию в монитор порта
    out_debug_str = "Sent -> " + out_espnow_string;
}
# Что получилось:
Блок отслеживает изменения на 6 входах.

При изменении одного датчика формирует компактную строку (например, "F1:25.50").

Эта строка подается на выход out_espnow_string, который нужно будет подключить к строковому входу (String) блока ESP_NOW ESP8266 (передатчик).

Проверьте и сохраните блок. После этого мы создадим DECODER_ESP_NEW для разбора такой строки на стороне приемника.
# блока DECODER_ESP_NEW
## Код для DeclareSection
// Переменные для хранения последних разобранных значений
float last_f1 = 0, last_f2 = 0, last_f3 = 0;
int last_i1 = 0, last_i2 = 0, last_i3 = 0;
## Код для LoopSection
// ОСНОВНАЯ ЛОГИКА: Активируется по переднему фронту in_trigger
if (in_trigger && in_espnow_string.length() > 0) {
    // Ищем разделитель ':'
    int colonIndex = in_espnow_string.indexOf(':');
    
    if (colonIndex != -1) {
        // Разделяем строку на ключ и значение
        String key = in_espnow_string.substring(0, colonIndex);
        String valStr = in_espnow_string.substring(colonIndex + 1);
        
        // Преобразуем строку значения в число (float)
        float value = valStr.toFloat();
        
        // Распределяем значение по ключу
        if (key == "F1") {
            last_f1 = value;
            out_sensor1_float = last_f1;
            out_debug_str = "D: F1=" + String(last_f1, 2);
        }
        else if (key == "F2") {
            last_f2 = value;
            out_sensor2_float = last_f2;
            out_debug_str = "D: F2=" + String(last_f2, 2);
        }
        else if (key == "F3") {
            last_f3 = value;
            out_sensor3_float = last_f3;
            out_debug_str = "D: F3=" + String(last_f3, 2);
        }
        else if (key == "I1") {
            last_i1 = (int)value; // Преобразуем float обратно в int
            out_sensor1_int = last_i1;
            out_debug_str = "D: I1=" + String(last_i1);
        }
        else if (key == "I2") {
            last_i2 = (int)value;
            out_sensor2_int = last_i2;
            out_debug_str = "D: I2=" + String(last_i2);
        }
        else if (key == "I3") {
            last_i3 = (int)value;
            out_sensor3_int = last_i3;
            out_debug_str = "D: I3=" + String(last_i3);
        }
        else {
            out_debug_str = "D: Unknown key -> " + in_espnow_string;
        }
    } else {
        out_debug_str = "D: Invalid format -> " + in_espnow_string;
    }
}
#  Итоговая архитектура на приёмной стороне:
[ESP_NOW ESP8266 (Режим "Прием")] --> (Выход String) --> [DECODER_ESP_NEW.in_espnow_string]
                                                               |
                                            [DECODER_ESP_NEW.out_sensor1_float] --> [Nextion]
                                            [DECODER_ESP_NEW.out_sensor2_float] --> [Nextion]
                                                               ... и т.д.
## Архитектура отладки на приёмной стороне
[ESP_NOW ESP8266 (Receiver)] --- (debug String) ---> [Software UART 1] ---> Монитор порта (Строка 1)
         ↓
(espnow_string) ---> [DECODER_ESP_NEW] --- (out_debug_str) ---> [Software UART 2] ---> Монитор порта (Строка 2)

# Настройка блока ESP_NOW ESP8266 (приёмник)
В параметрах блока активируйте флажок для создания выхода debug (см. пункт 2.3 описания: "debug - выход для вывода информации при отладке...").

В проекте FLProg:

Добавьте блок Software UART.

Подключите выход debug блока ESP_NOW ESP8266 ко входу блока Software UART.

В настройках Software UART выберите, например, UART1 и скорость (115200).

Теперь в Мониторе порта вы будете видеть системные сообщения от ESP-NOW.
# Настройка DECODER_ESP_NEW для отладки
Мы уже предусмотрели выход out_debug_str. Нужно вывести его на второй виртуальный порт.

В проекте FLProg:

Добавьте второй блок Software UART (или используйте другой канал того же блока, если он поддерживает несколько).

Подключите выход out_debug_str блока DECODER_ESP_NEW ко входу этого Software UART.

В настройках выберите, например, UART2.
# Пример кода для LoopSection DECODER_ESP_NEW (улучшенная отладка)
Чтобы в Мониторе порта было понятнее, можно немного изменить строку отладки в коде LoopSection DECODER_ESP_NEW. Найдите блоки out_debug_str = "D: F1=..." и замените формирование строки на более информативное:
# LoopSection блока DECODER_ESP_NEW:
// ОСНОВНАЯ ЛОГИКА: Активируется по переднему фронту in_trigger
if (in_trigger && in_espnow_string.length() > 0) {
    // Ищем разделитель ':'
    int colonIndex = in_espnow_string.indexOf(':');
    
    if (colonIndex != -1) {
        // Разделяем строку на ключ и значение
        String key = in_espnow_string.substring(0, colonIndex);
        String valStr = in_espnow_string.substring(colonIndex + 1);
        
        // Преобразуем строку значения в число (float)
        float value = valStr.toFloat();
        
        // Распределяем значение по ключу и формируем ОДНУ отладочную строку
        if (key == "F1") {
            last_f1 = value;
            out_sensor1_float = last_f1;
            out_debug_str = "[DECODER] Key:F1 Val:" + String(last_f1, 2);
        }
        else if (key == "F2") {
            last_f2 = value;
            out_sensor2_float = last_f2;
            out_debug_str = "[DECODER] Key:F2 Val:" + String(last_f2, 2);
        }
        else if (key == "F3") {
            last_f3 = value;
            out_sensor3_float = last_f3;
            out_debug_str = "[DECODER] Key:F3 Val:" + String(last_f3, 2);
        }
        else if (key == "I1") {
            last_i1 = (int)value;
            out_sensor1_int = last_i1;
            out_debug_str = "[DECODER] Key:I1 Val:" + String(last_i1);
        }
        else if (key == "I2") {
            last_i2 = (int)value;
            out_sensor2_int = last_i2;
            out_debug_str = "[DECODER] Key:I2 Val:" + String(last_i2);
        }
        else if (key == "I3") {
            last_i3 = (int)value;
            out_sensor3_int = last_i3;
            out_debug_str = "[DECODER] Key:I3 Val:" + String(last_i3);
        }
        else {
            out_debug_str = "[DECODER] ERR: Unknown key -> " + in_espnow_string;
        }
    } else {
        out_debug_str = "[DECODER] ERR: Invalid format -> " + in_espnow_string;
    }
}
                                                               
#  Порядок тестирования
Соберите схему передатчика (CODER>ESP_NEW → ESP_NOW (Transmitter)).

Соберите схему приёмника как указано выше.

Откройте Монитор порта (два окна для двух UART или один с фильтрацией).

Подайте изменение на датчик передатчика. Должны увидеть:

Сообщение о отправке на стороне передатчика.

Сообщение о приёме от ESP_NOW (Receiver).

Сообщение "[DECODER] Key:..." о успешном разборе.
 # Ключевое изменение:
 Все 6 строк out_debug_str = "D: F1=... заменены на единый формат "[DECODER] Key:XX Val:...".

Добавлены префиксы [DECODER] для фильтрации и ERR: для ошибок.
